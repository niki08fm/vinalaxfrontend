# Base URL for the Client Portal proxy routes (vinalaxbackend's /api/client/*).
# Defaults to the deployed Render backend if unset — override for local dev.
VITE_CLIENT_API_URL=http://localhost:5002/api/client
